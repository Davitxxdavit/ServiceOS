-- Combined init migration for Supabase CLI.
-- Equivalent to applying schema.sql + policies/rls.sql + storage/menu-images.sql.
-- For readability, prefer those source files; keep this migration in sync when changing schema.

-- See ../schema.sql, ../policies/rls.sql, ../storage/menu-images.sql
select 1;
